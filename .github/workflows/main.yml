# .github/workflows/deploy.yml
name: Deploy 🚀 Clima to Google Cloud Run (☁🏃)
on:
  push:
    paths: 
      - 'Phase 1/**'

jobs:
  deploy:
    name: Deploying 🚀 Clima
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      # Build and push image to Google Container Registry
      - name: Setting up
        uses: google-github-actions/setup-gcloud@main
        with:
          version: '286.0.0'
          service_account_key: ${{ secrets.GCP_SA_KEY_JSON }}
          service_account_email: "oskarsczyz@gmail.com"
          project_id: bcccodingchallenge

      - name: Building (🏗)
        run: |-
          gcloud builds submit --tag gcr.io/bcccodingchallenge/netcoreapi:test5

      # Setup gcloud CLI
      - name: Deploy (🚀)
        uses: google-github-actions/deploy-cloudrun@main
        with:
          service: netcoreapi:test5
          image: gcr.io/bcccodingchallenge/netcoreapi:test5
          region: europe-central2
          credentials: ${{ secrets.GCP_SA_KEY_JSON }}
          project_id: bcccodingchallenge
